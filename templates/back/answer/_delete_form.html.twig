    <form method="post" action="{{ path('app_back_answer_delete', {'id': answer.id}) }}" onsubmit="return confirm('Étes vous sûr de vouloir supprimer cette réponse ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ answer.id) }}">
        <button class="btn btn_delete mx-3">Supprimer</button>
    </form>
</div>
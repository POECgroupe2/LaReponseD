{% extends 'base.html.twig' %}

{% block title %}Edition
	{{ level.name }}
{% endblock %}

{% block body %}
	{% include 'back/partial/back_nav_bar.html.twig' %}
	<div class="container pt-5">
		<div class="row mb-2">
			<div class="col-8 mx-auto">
				<h1 class="text-center my-5">Modification de
					{{ level.name }}</h1>
				<a class="btn btn_return mb-5" href="{{ path('app_back_level_index') }}">Retour à la liste</a>
				<div class="col">
					{{ include('back/level/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
					{{ include('back/level/_delete_form.html.twig') }}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
